node_modules
package-lock.json

# 산출물 — prerender.js 가 만든다. 정렬하면 원본과 어긋난다
public/prerender

# 문서 — 표 정렬·줄바꿈을 손으로 맞춰 둔 것이 다시 흐트러진다
*.md
.claude

# 컴파일 산출물 — Watch Sass 가 만든다. 정렬하면 다음 컴파일에 되돌아가 diff 만 생긴다
public/css

# 외부 라이브러리 원본
*.min.js
